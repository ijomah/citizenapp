import { RemoveParagraphTagPipe } from './remove-paragraph-tag.pipe';

describe('RemoveParagraphTagPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveParagraphTagPipe();
    expect(pipe).toBeTruthy();
  });
});
